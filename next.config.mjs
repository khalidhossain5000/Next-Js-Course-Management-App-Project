/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
        'lh3.googleusercontent.com',
          'i.ibb.co',
    ], // Google profile images
  },
};

export default nextConfig;
